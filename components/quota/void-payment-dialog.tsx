// Superato: usava voidPayment() del vecchio schema. L'equivalente oggi e'
// reversePayment() in lib/actions/payments.ts — non ancora ricollegato a
// una UI (nessuno storico pagamenti per rata e' ancora mostrato).
export {};
